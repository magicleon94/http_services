name: http_services
description: A Dart package based on Dio that provides base models to handle HTTP services requests.
version: 2.0.2
homepage: https://github.com/magicleon94/http_services

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  dio: ^4.0.0
  disposable_object: ^2.0.0
  meta: ^1.3.0

dev_dependencies:
  pedantic: ^1.8.0
  test: ^1.15.7
  http_mock_adapter: ^0.2.1